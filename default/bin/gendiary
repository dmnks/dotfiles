#!/bin/bash
# Generate a vimwiki diary entry from a template.

DAY=$1
WEEKDAY=$(date -d"$DAY" +"%a")
echo "= $WEEKDAY $DAY ="
echo
TEMPLATE=$HOME/vimwiki/diary/templates/$WEEKDAY.wiki
[[ -f "$TEMPLATE" ]] && cat $TEMPLATE
