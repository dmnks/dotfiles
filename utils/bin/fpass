#!/bin/bash
# Inspired by https://bargsten.org/bash/fzf-pass-tool/
pwdir=${PASSWORD_STORE_DIR-~/.password-store}
match=$(find $pwdir -name "*.gpg" |
        perl -pE "s#^(?:.*)\\Q$pwdir\\E/(.*)\.gpg#\\1#" |
        fzf --layout=reverse);
[ -n "$match" ] && pass -c $match
