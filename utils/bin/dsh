#!/bin/bash

PROGRAM=$(basename $0)
PROJECT=$(basename $PWD)
IMAGE=$PROGRAM/$PROJECT
CMD=$1; shift

if [ -z "$CMD" ]; then
    if ! podman image exists $IMAGE; then
	echo "No image $IMAGE" >&2
	exit 1
    fi
    if podman container exists $PROJECT; then
        podman start $PROJECT >/dev/null
        podman attach $PROJECT
    else
	podman run \
	    -it -e TERM \
	    --name $PROJECT \
	    --hostname $PROJECT \
	    --privileged \
	    --volume=$PWD:/src \
	    --volume=$PWD/.data:/data \
	    --detach-keys="ctrl-@" \
	    --workdir /src \
	    $IMAGE
    fi
elif [ "$CMD" == "rm" ]; then
    podman container exists $PROJECT && podman rm $PROJECT
fi
